<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/reflectionLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/aurora_gradient_light"
    tools:context=".ReflectionActivity">

    <!-- Title -->
    <TextView
        android:id="@+id/reflectionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Mood Reflection"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

    <!-- Placeholder for reflection content -->
    <TextView
        android:id="@+id/reflectionContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Reflect on your mood here..."
        android:textColor="@color/deep_night_blue"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/reflectionTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:padding="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
